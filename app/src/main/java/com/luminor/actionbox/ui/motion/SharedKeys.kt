package com.luminor.actionbox.ui.motion

object SharedKeys {
    fun action(id: Long) = "action-$id"
    fun project(id: Long) = "project-$id"
    fun saved(id: Long) = "saved-$id"
    const val Capture = "capture-card"
}
